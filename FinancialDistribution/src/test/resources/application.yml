spring:
  datasource:
    url: jdbc:h2:mem:DistributionDbTEST
    #initialization-mode: always
    data: data.sql

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
      show-sql: false

logging:
  level:
    ROOT: ERROR
